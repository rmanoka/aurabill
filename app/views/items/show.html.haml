
= javascript_tag do
  window.items =
  = raw @item.sub_items.to_json(:only => [ :id ])
  

.row
  .large-12.columns
    %h1.underline
      = @item.name


.row
  .large-2.columns
    %h3.subheader
      Go back
  .large-10.columns.item
    .panel
      = render partial: 'item_view', object: @item.parent, as: 'item'
      
.flex
  - @item.sub_items.each do |i|
    .panel
      = render partial: 'item_view', object: i, as: 'item'

